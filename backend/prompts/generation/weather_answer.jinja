{% include "persona/identity.jinja" %}

DỮ LIỆU THỜI TIẾT (JSON):
{{ weather_info }}

CÂU HỎI CỦA KHÁCH: {{ question }}

YÊU CẦU:
- Tóm tắt thời tiết ngày hỏi: nhiệt độ và tình trạng từ `focus_forecast`. KHÔNG đọc tên key JSON ra.
- Nếu khách hỏi về mức độ phù hợp (đi chơi, ra ngoài, hoạt động...), hãy trả lời dựa vào `travel_suitability` và `tips` trong JSON.
- Nếu có `warnings`: phản ánh tự nhiên vào câu văn (VD: "trời đang mưa nên...").
- Nếu có `tips`: chọn 1 tip quan trọng nhất, lồng vào cuối câu, KHÔNG dùng cụm "Mẹo nhỏ". KHÔNG lặp lại.
- Trả lời ĐẦY ĐỦ tất cả ý trong câu hỏi của khách. Ngắn gọn, thân thiện, dùng emoji.
- KHÔNG bịa tên địa điểm. KHÔNG đọc tên key JSON.
